# myfile

python manage.py runserver
python -m pip install Django
python -m venv .venv
STATIC_ROOT = BASE_DIR/'static'
/home/imLincoln/Name_ID_JobPortal/static'
/static/
python manage.py collectstatic
ALLOWED_HOSTS = ["imlincoln.pythonanywhere.com"]
